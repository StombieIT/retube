.wrapper {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 20px;

    :global(.candidate) {
        flex-basis: 250px;
    }
}

.logo {
    align-self: center;
    margin-bottom: 30px;
}

.header {
    align-self: center;
    margin-bottom: 70px;
    font-size: 22px;
}
