$common-br = 20px;

.wrapper {
    display: flex;
    flex-direction: column;
}

.controls {
    display: flex;
    justify-content: space-between;
}

.button {
    cursor: pointer;
    padding: 10px;
    border: none;
    color: #ffffff;
    background-color: transparent;
}
